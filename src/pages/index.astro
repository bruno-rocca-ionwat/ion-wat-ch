---
import Layout from '../layouts/Layout.astro';
import ContextAlert from '../components/ContextAlert.astro';
import Markdown from '../components/Markdown.astro';

const explainer = `
IONWATCH systems,designed in Europe,made in America, track the energy production for solar,wind and wave.

<!-- Netlify supports not only [Streaming SSR](https://docs.astro.build/en/guides/server-side-rendering/#html-streaming) and fast [Edge Middleware](https://docs.astro.build/en/guides/middleware/), [...]
Any page or data can be rebuilt only when needed, without site visitors ever getting a performance hit.  -->
`;
---
<style>
  .background-video {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -1; /* Ensures the video is behind other content */
  }
  .overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(129, 216, 208, 0.07); /* 95% transparent white */
    z-index: 0; /* Ensures the overlay is above the video but behind the content */
  }
  .content {
    position: relative;
    z-index: 1; /* Ensures the content is above the overlay */
  }
  .btn {
    z-index: 2; /* Ensure buttons are above the content */
    display: inline-block;
    padding: 10px 20px;
    font-size: 16px;
    color: white;
    background-color: #81d8d0;
    border: none;
    border-radius: 4px;
    text-align: center;
    text-decoration: none;
    cursor: pointer;
  }
  .btn:focus, .btn:hover {
    background-color: #81d8d6;
  }
</style>

<video class="background-video" autoplay muted loop playsinline>
  <source src="/videos/ionwatch-background.mp4" type="video/mp4" />
  Your browser does not support the video tag.
</video>
<div class="overlay"></div>
<Layout title="Welcome to OutreLA.">
  <div class="content">
    <ContextAlert class="mb-8" />
    <h1 class="mb-10">CONNECTED SENSORS for INDUSTRIAL SYSTEMS</h1>
    <Markdown content={explainer} class="mb-10 text-lg" />
    <p>
      <a href="https://ionwat.ch/learnMore" class="btn btn-lg sm:min-w-64">Learn More</a>
    </p>
  </div>
</Layout>
